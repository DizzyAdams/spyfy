# SpyFy API — dependências de PRODUÇÃO (imagem Docker / deploy em PaaS free).
# Conjunto enxuto: cobre 100% dos endpoints (/health, /v1/offers, /v1/agents/*,
# /v1/notify, /v1/webhooks) rodando 100% OFFLINE (embeddings determinísticos do
# chromadb, sem GPU/LLM). LangChain/whisper/playwright NÃO são necessários no
# path da API e ficam de fora de propósito para caber no free tier.
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn>=22.0
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
tenacity>=8.3
apprise>=1.8
# RAG memory (offline) + grafo LangGraph dos agentes autônomos
chromadb>=0.5
langgraph>=0.2

