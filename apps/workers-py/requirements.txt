# SpyFy API — dependências do deploy SERVERLESS (Vercel Python, limite 225MB).
# Conjunto enxuto: cobre os endpoints consumidos pelo frontend
# (/health, /v1/version, /v1/events/types, /v1/offers, /v1/metrics,
# /v1/offers/estimate, /v1/notify, /v1/webhooks). Os agents autônomos
# (chromadb/langgraph) são importados LAZY e degradam graciosamente quando
# ausentes (veja spyfy/api/app.py).
#
# NOTA Scrapling: o pacote scrapling + playwright/patchright excede o limite
# de 225MB do Vercel Python serverless, então NÃO entra aqui. O
# scrapling_adapter.py detecta a ausência (SCRAPLING_AVAILABLE=False) e degrada
# para httpx.Client — o feed continua funcional (fallback estruturado) no
# serverless. Em Docker/Render/local (requirements.docker.txt) o Scrapling está
# presente e raspa as Ad Libraries de verdade (com browser + cookies.json ou
# token de API). O smoke test de prova é apps/workers-py/_smoke_scraper.py.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
tenacity>=8.3
apprise>=1.8
mangum>=0.17
