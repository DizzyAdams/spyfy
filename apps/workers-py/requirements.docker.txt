# SpyFy API — dependências (imagem Docker / self-host / Coolify).
# Memória RAG leve: SQLite + numpy puro (sem chromadb, ~10MB RAM).
# Grafo LangGraph dos agentes autônomos (/v1/agents/*).
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn>=22.0
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
tenacity>=8.3
apprise>=1.8
# RAG memory determinística (offline) + grafo de agentes
numpy>=1.26
langgraph>=0.2
# langgraph>=0.2 resolve para 1.x, que exige langchain-core>=1.4.7
# (a API do Reviver(allowed_objects=...) mudou em langchain-core 1.4.x).
# Sem esse pin o pip pode travar langchain-core 0.1.x e quebrar o import
# do grafo de agentes (Reviver.__init__ got unexpected keyword 'allowed_objects').
langchain-core>=1.4.7,<2
langchain>=0.3
